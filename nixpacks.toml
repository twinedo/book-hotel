[phases.setup]
packages = ["unzip", "bun", "debian-openssl-3.0.x"]

[phases.build]
commands = [
  "cd apps/backend && bun install --production",
  "cd apps/backend && bunx prisma generate",
  "cd apps/backend && bun run build"
]

[phases.start]
command = "./apps/backend/server"